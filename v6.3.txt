ta">0 MB</strong><span id="v6DataSplit">↑ 0 MB · ↓ 0 MB</span></div><div class="session-metric"><small>BITRATE TX · MÍN / MÁX</small><strong id="v6TxRange">—</strong></div><div class="session-metric"><small>BITRATE RX · MÍN / MÁX</small><strong id="v6RxRange">—</strong></div><div class="session-metric"><small>LATENCIA · MÍN / MÁX</small><strong id="v6RttRange">—</strong><span id="v6RttAvg">Media —</span></div><div class="session-metric"><small>INTERRUPCIONES</small><strong id="v6Cuts">0</strong><span id="v6Last">Sin cortes detectados.</span></div></div><div class="health-card" id="v6HealthCard"><div class="health-copy"><small>DIAGNÓSTICO</small><strong id="v6Health">Excelente</strong><p id="v6Bottleneck">Sin cuello de botella detectado.</p></div><div class="health-score" id="v6Score">100</div><div class="health-meter"><span id="v6Bar"></span></div></div></div>`}
function incident(m){if(!callActive())return;session.cuts++;session.last=`${new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'})} · ${m}`;renderSession()}
function setTxt(id,v){const e=$(id);if(e)e.textContent=v}
function renderSession(){setTxt('#v6Data',fmtBytes(session.sent+session.recv));setTxt('#v6DataSplit',`↑ ${fmtBytes(session.sent)} · ↓ ${fmtBytes(session.recv)}`);setTxt('#v6TxRange',Number.isFinite(session.txMin)?`${fmtRate(session.txMin)} / ${fmtRate(session.txMax)}`:'—');setTxt('#v6RxRange',Number.isFinite(session.rxMin)?`${fmtRate(session.rxMin)} / ${fmtRate(session.rxMax)}`:'—');setTxt('#v6RttRange',Number.isFinite(session.rttMin)?`${Math.round(session.rttMin)} / ${Math.round(session.rttMax)} ms`:'—');setTxt('#v6RttAvg',session.rttN?`Media ${Math.round(session.rttSum/session.rttN)} ms`:'Media —');setTxt('#v6Cuts',String(session.cuts));setTxt('#v6Last',session.last)}
function statRate(key,abs,ts){const p=session.prevRate.get(key);session.prevRate.set(key,{b:abs||0,t:ts});return p&&ts>p.t&&abs>=p.b?(abs-p.b)*8000/(ts-p.t):null}
async function collect(){if(!callActive())return;const tx={rate:0,saw:false,w:0,h:0,fps:0,codec:'',loss:null,jitter:null,reason:'none'},rx={rate:0,saw:false,w:0,h:0,fps:0,codec:'',loss:null,jitter:null};let rtt=null,freeze=false;for(const pc of pcs){if(pc.connectionState==='closed')continue;let rep;try{rep=await pc.getStats()}catch{continue}const by=new Map();rep.forEach(x=>by.set(x.id,x));rep.forEach(x=>{const k=`${pc.__luma6}:${x.id}`;if((x.type==='outbound-rtp'||x.type==='inbound-rtp')&&!x.isRemote){const out=x.type==='outbound-rtp',abs=out?x.bytesSent:x.bytesReceived,p=session.prevBytes.get(`${k}:${out?'o':'i'}`);if(Number.isFinite(abs)){if(Number.isFinite(p)&&abs>=p)out?session.sent+=abs-p:session.recv+=abs-p;session.prevBytes.set(`${k}:${out?'o':'i'}`,abs)}}if(x.type==='outbound-rtp'&&x.kind==='video'&&!x.isRemote){const v=statRate(`${k}:tx`,x.bytesSent,x.timestamp);if(Number.isFinite(v)){tx.rate+=v;tx.saw=true}const c=by.get(x.codecId);if(c?.mimeType)tx.codec=c.mimeType.replace('video/','');tx.w=x.frameWidth||tx.w;tx.h=x.frameHeight||tx.h;tx.fps=x.framesPerSecond||tx.fps;if(x.qualityLimitationReason&&x.qualityLimitationReason!=='none')tx.reason=x.qualityLimitationReason}if(x.type==='remote-inbound-rtp'&&x.kind==='video'){const total=(x.packetsLost||0)+(x.packetsReceived||0);if(total>0)tx.loss=(x.packetsLost||0)/total*100;if(Number.isFinite(x.jitter))tx.jitter=x.jitter*1000;if(Number.isFinite(x.roundTripTime))rtt=x.roundTripTime*1000}if(x.type==='inbound-rtp'&&x.kind==='video'&&!x.isRemote){const v=statRate(`${k}:rx`,x.bytesReceived,x.timestamp);if(Number.isFinite(v)){rx.rate+=v;rx.saw=true}const c=by.get(x.codecId);if(c?.mimeType)rx.codec=c.mimeType.replace('video/','');rx.w=x.frameWidth||rx.w;rx.h=x.frameHeight||rx.h;rx.fps=x.framesPerSecond||rx.fps;const total=(x.packetsLost||0)+(x.packetsReceived||0);if(total>0)rx.loss=(x.packetsLost||0)/total*100;if(Number.isFinite(x.jitter))rx.jitter=x.jitter*1000;if(Number.isFinite(x.freezeCount)){const p=session.prevFreeze.get(k);if(Number.isFinite(p)&&x.freezeCount>p)freeze=true;session.prevFreeze.set(k,x.freezeCount)}}if(x.type==='candidate-pair'&&x.state==='succeeded'&&(x.nominated||x.selected)&&Number.isFinite(x.currentRoundTripTime))rtt=x.currentRoundTripTime*1000})}
if(!tx.saw)tx.rate=null;if(!rx.saw)rx.rate=null;const ls=localTrack()?.getSettings?.()||{};tx.w||=ls.width||0;tx.h||=ls.height||0;tx.fps||=ls.frameRate||0;setTxt('#v6TxRes',tx.w&&tx.h?`${tx.w}×${tx.h}`:'—');setTxt('#v6TxCodec',tx.codec||'WebRTC');setTxt('#v6TxRate',fmtRate(tx.rate));setTxt('#v6TxFps',tx.fps?`${Math.round(tx.fps)} FPS`:'—');setTxt('#v6TxLoss',tx.loss!=null?`${tx.loss.toFixed(tx.loss<1?2:1)}%`:'—');setTxt('#v6TxJitter',tx.jitter!=null?`${Math.round(tx.jitter)} ms`:'—');setTxt('#v6Limit',tx.reason==='cpu'?'Codificador':tx.reason==='bandwidth'?'Red':'Sin limitación');setTxt('#v6RxRes',rx.w&&rx.h?`${rx.w}×${rx.h}`:'—');setTxt('#v6RxCodec',rx.codec||'WebRTC');setTxt('#v6RxRate',fmtRate(rx.rate));setTxt('#v6RxFps',rx.fps?`${Math.round(rx.fps)} FPS`:'—');setTxt('#v6RxLoss',rx.loss!=null?`${rx.loss.toFixed(rx.loss<1?2:1)}%`:'—');setTxt('#v6RxJitter',rx.jitter!=null?`${Math.round(rx.jitter)} ms`:'—');if(tx.rate>50000){session.txMin=Math.min(session.txMin,tx.rate);session.txMax=Math.max(session.txMax,tx.rate)}if(rx.rate>50000){session.rxMin=Math.min(session.rxMin,rx.rate);session.rxMax=Math.max(session.rxMax,rx.rate)}if(rtt>0){session.rttMin=Math.min(session.rttMin,rtt);session.rttMax=Math.max(session.rttMax,rtt);session.rttSum+=rtt;session.rttN++